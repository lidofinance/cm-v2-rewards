# CM Rewards Actions

This branch contains only GitHub Actions wrappers for publishing CM rewards artifacts.

The implementation lives in [`lidofinance/csm-rewards`](https://github.com/lidofinance/csm-rewards). If you want to run the code manually, follow the instructions in that repository instead.

Generated artifacts are published to the `mainnet` and `hoodi` branches of this repository.
